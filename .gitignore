# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/

# Environment variables
.env
.env.local
.env.*.local
.env.test
# E2E local (7D.2): lleva la anon key y la service_role del stack local.
# El .example sí se versiona.
.env.e2e
# storageState de Playwright: sesión autenticada del usuario E2E sembrado.
/tests/e2e/.auth/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Supabase
supabase/.branches
supabase/.temp

# Deno — el repo no fija versiones de imports remotos vía deno.json; el
# lockfile se autogenera al correr `deno check` localmente y no debe versionarse.
deno.lock

# Build-time generated files (no commitear — se regeneran en cada build)
public/version.json

# Playwright E2E — artefactos de ejecución local (nunca commitear)
playwright-report/
test-results/
